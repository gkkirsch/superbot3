# Dependencies
node_modules/

# Build output
broker/dashboard-ui/dist/

# Runtime
*.pid
.tmp/

# Credentials (never commit)
.credentials.json
.claude.json

# Lockfiles
*.lock

# Data directories (created by `superbot3 init`, not part of code)
spaces/
config.json

# Orchestrator runtime data
orchestrator/.claude/teams/
orchestrator/.claude/projects/
orchestrator/.claude/backups/
orchestrator/.claude/.credentials.json
orchestrator/.claude/.claude.json
orchestrator/.claude/history.jsonl
orchestrator/.claude/mcp-needs-auth-cache.json
orchestrator/.claude/shell-snapshots/
orchestrator/.claude/cache/
orchestrator/.claude/plugins/

# Templates runtime copies (regenerated by init)
templates/default/

# Claude Code local overrides (project-level agents/skills added by user)
.agents/
/skills/
.claude/skills/shadcn

# IDE / editor
.windsurf/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Skills lock
skills-lock.json
